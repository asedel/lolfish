# This is handled inside fish_mode_prompt (see `__myfish_prompt_vi`)
#
# If you want to override this with your own mode prompt, disable myfish's
# built-in mode prompt in your fish config:
#
#     set -g theme_display_vi no

function fish_mode_prompt
end
